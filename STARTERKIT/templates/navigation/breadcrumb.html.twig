{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {% include "@STARTERKIT/navigation/breadcrumb/breadcrumb.twig" with {title: 'Breadcrumb'|t} %}
{% endif %}
